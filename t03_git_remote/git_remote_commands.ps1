# Remote commands for git
# Peadar Grant

# make your own local working copy of a remote repository
cd parent/folder/of/repository
git clone https://url/of/repository
# only need to do this once

# get changes from remote repository
git pull # always do git pull before starting work locally!

# send your changes to the remote repository
git push # always do git push after you commit!


